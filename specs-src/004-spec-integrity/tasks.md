# Tasks: Spec-Integrity Gate (SLOCK)

Every task traces to exactly one requirement (3PWR-FR-016) and declares its file scope
(3PWR-FR-017). Two-way coverage with the spec is checked by `3pwr coverage-check` (3PWR-FR-015).

- [x] T001 [SLOCK-FR-001] Seal the full-document SHA-256 + root-relative path (+ sign-off commit) into the signed Spec-stage `signoff` payload, on both the manual and the orchestration path (files: engine/src/threepowers/speclock.py, engine/src/threepowers/cli.py, engine/tests/test_speclock.py, engine/tests/integration/test_speclock_integration.py, engine/tests/e2e/test_speclock_e2e.py)
- [x] T002 [SLOCK-FR-002] Derive the current approved-spec hash from the committed ledger alone — latest Spec-stage sign-off carrying a `spec_hash`, no file I/O (files: engine/src/threepowers/speclock.py, engine/tests/test_speclock.py)
- [x] T003 [SLOCK-FR-003] Implement the `spec_integrity` gate: fail with class `spec_modified` naming the approving seq on a post-approval change; pass when unmodified; skip (never block) with an informational note when no approval exists (files: engine/src/threepowers/speclock.py, engine/src/threepowers/gates.py, engine/tests/test_speclock.py, engine/tests/integration/test_speclock_integration.py)
- [x] T004 [SLOCK-FR-004] Order the gate cheapest-first — after types, before tests — in the canonical gate order and every tier's gate list (files: engine/src/threepowers/verdict.py, .3powers/config/risk-tiers.yaml, engine/src/threepowers/scaffold/config/risk-tiers.yaml, .3powers/schemas/verdict.schema.json, engine/tests/test_speclock.py)
- [x] T005 [SLOCK-FR-005] Re-execute the spec-integrity check at `advance`; refuse `spec_modified` unless an active, signed `spec_integrity` deviation covers it; revoke/expiry re-blocks (files: engine/src/threepowers/cli.py, engine/tests/integration/test_speclock_integration.py)
- [x] T006 [SLOCK-FR-006] Let a fresh Spec-stage sign-off supersede the previous approval hash — latest wins across query, gate, and advance (files: engine/src/threepowers/speclock.py, engine/tests/test_speclock.py, engine/tests/integration/test_speclock_integration.py, engine/tests/e2e/test_speclock_e2e.py)
- [x] T007 [SLOCK-FR-007] Add the read-only `3pwr spec diff` command: exit 0 on match, non-zero + report (with a `git show` textual diff when the sign-off commit is known) on mismatch; never writes to the ledger (files: engine/src/threepowers/cli.py, engine/tests/integration/test_speclock_integration.py, engine/tests/e2e/test_speclock_e2e.py)
- [x] T008 [SLOCK-NFR-001] Keep the check deterministic — raw-bytes hashing, gate result a pure function of ledger state + file bytes; no model call, no network (files: engine/src/threepowers/speclock.py, engine/tests/test_speclock.py)
- [x] T009 [SLOCK-NFR-002] Prove tamper-evidence: the approval hash lives inside the signed sign-off entry, so altering it fails `3pwr verify` with no new verification code (files: engine/tests/integration/test_speclock_integration.py)
- [x] T010 [SLOCK-NFR-003] Skip in O(1) when no approval hash exists — the ledger is consulted before the spec file is ever read; hold speclock to the High-risk bar (mutation scope) (files: engine/src/threepowers/speclock.py, engine/pyproject.toml, engine/tests/test_speclock.py)
- [x] T011 [SLOCK-FR-007] Document the sealing sign-off, the `spec_integrity` gate, and `3pwr spec diff` across the user-facing docs + plan/tasks artifacts (files: docs/cli-reference.md, docs/STATUS.md, AGENTS.md, CLAUDE.md, CHANGELOG.md, plan/016-spec-integrity.md, specs/004-spec-integrity/tasks.md)
