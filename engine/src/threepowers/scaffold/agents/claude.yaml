# Agent backend manifest — Claude Code, headless (EXEC-FR-004). Seeded by `3pwr init`.
# The engine makes no model call itself; the dispatched `claude` process does (EXEC-NFR-001). Point it at
# an enterprise gateway via env (ANTHROPIC_BASE_URL, CLAUDE_CODE_USE_BEDROCK/_VERTEX, HTTPS_PROXY) — the
# engine passes the environment through untouched (EXEC-FR-012).
agent: claude
family: anthropic
headless: true
command: claude
base_args: ["--permission-mode", "acceptEdits"]
model_flag: "--model"
prompt_flag: "-p"
prompt_via: arg
