# Tasks: Trust-Spine Hardening (HARDN)

Every task traces to exactly one requirement (3PWR-FR-016) and declares its file scope
(3PWR-FR-017). Two-way coverage with the spec is checked by `3pwr coverage-check` (3PWR-FR-015).

- [x] T001 [HARDN-FR-001] Write the versioned threat-model document — what each trust-spine mechanism proves, the tamper classes `verify` detects and cannot detect, the custody boundary, and the self-reported oracle-claim caveat — linked from README and SECURITY, asserted by a docs-conformance test (files: docs/threat-model.md, README.md, SECURITY.md, engine/tests/test_custody.py)
- [x] T002 [HARDN-FR-002] Refuse in-repo key creation in `keygen`/`rotate-key` and run a custody preflight in `verify`: a private key inside the working tree or readable by other users emits a failing `key_custody` finding; a compliant setup emits nothing (files: engine/src/threepowers/keys.py, engine/src/threepowers/cli.py, engine/src/threepowers/scaffold.py, engine/tests/test_custody.py, engine/tests/e2e/test_hardening_e2e.py)
- [x] T003 [HARDN-FR-003] Add the always-on core `ed25519-priv` check to the secret gate — fails the verdict naming the file, with or without an external scanner; only the external ruleset portion quarantines (files: engine/src/threepowers/scanners.py, engine/tests/test_custody.py, engine/tests/e2e/test_hardening_e2e.py)
- [x] T004 [HARDN-FR-004] Implement key rotation as a signed `key_rotation` entry authored by the outgoing key, span-based verification of the succession, the unrotated-key-change finding, and the `3pwr rotate-key` command (files: engine/src/threepowers/ledger.py, engine/src/threepowers/verify.py, engine/src/threepowers/cli.py, .3powers/schemas/ledger-entry.schema.json, engine/tests/test_rotation.py, engine/tests/e2e/test_hardening_e2e.py)
- [x] T005 [HARDN-FR-005] Implement opt-in anchoring: `3pwr anchor` records the head as the git tag `3powers/anchor/<seq>` + a local receipt (`--push` is the only network op); `3pwr verify --anchored` fails on truncation/rewrite behind the anchor; plain verify unchanged and offline (files: engine/src/threepowers/anchor.py, engine/src/threepowers/ledger.py, engine/src/threepowers/cli.py, engine/tests/test_anchor.py, engine/tests/e2e/test_hardening_e2e.py)
- [x] T006 [HARDN-FR-006] Delegate signing to an external process boundary where configured: `Signer` protocol + `CommandSigner` via `$THREEPOWERS_SIGNER_CMD` / `$THREEPOWERS_ORACLE_SIGNER_CMD`, loud failure with no software fallback, verification unchanged (files: engine/src/threepowers/keys.py, engine/src/threepowers/ledger.py, engine/src/threepowers/provenance.py, engine/src/threepowers/cli.py, engine/tests/test_external_signer.py)
- [x] T007 [HARDN-FR-007] Cross-check the self-reported `oracle record` model against the ledger-attested dispatch integration — a contradiction blocks a High-risk advance; without a dispatch, `oracle verify` states the claim is self-reported (files: engine/src/threepowers/oracle.py, engine/tests/test_model_attestation.py)
- [x] T008 [HARDN-FR-008] Count a requirement as traced only when its ID is bound to a test declaration (name/declaration line or adjacent docstring); a comment-only mention fails as `untraced_requirement` naming the ID; engine + sample binding styles keep passing (files: engine/src/threepowers/conformance.py, engine/src/threepowers/gates.py, .3powers/adapters/CONTRACT.md, engine/tests/test_conformance_binding.py)
- [x] T009 [HARDN-FR-009] Require ≥1 assertion in each requirement-bound test with adapter-declared patterns; assertion-free bound tests fail as `weak_test` naming requirement + file; adapters without patterns quarantine visibly, riding the same single scan pass (files: engine/src/threepowers/conformance.py, .3powers/adapters/python/adapter.yaml, .3powers/adapters/typescript/adapter.yaml, .3powers/adapters/go/adapter.yaml, engine/src/threepowers/scaffold/adapters/python/adapter.yaml, engine/src/threepowers/scaffold/adapters/typescript/adapter.yaml, engine/src/threepowers/scaffold/adapters/go/adapter.yaml, engine/tests/test_conformance_binding.py)
- [x] T010 [HARDN-FR-010] Flag newly added assertion-free requirement-referencing tests (diff + untracked, suffix-aware declarations) as `gate_gaming` signals routed to mandatory human review, sanctionable only via the signed deviation path (files: engine/src/threepowers/gaming.py, engine/tests/test_antigaming.py)
- [x] T011 [HARDN-FR-011] Add the per-tier `diff_mutation` knob: with the knob set and a `--base` given, run mutation scoped to the changed source files against the tier's `mutation_score`; unset → unchanged; missing tool → quarantine (files: engine/src/threepowers/gates.py, .3powers/config/risk-tiers.yaml, engine/src/threepowers/scaffold/config/risk-tiers.yaml, engine/tests/test_antigaming.py, engine/tests/integration/test_diff_mutation_integration.py)
- [x] T012 [HARDN-NFR-001] Keep every check deterministic and the default verdict path offline — anchoring is the only network-capable feature, under its explicit opt-in commands (files: engine/src/threepowers/anchor.py, engine/tests/test_anchor.py, engine/tests/test_custody.py, engine/tests/test_model_attestation.py, engine/tests/test_conformance_binding.py, engine/tests/test_antigaming.py)
- [x] T013 [HARDN-NFR-002] Bound the always-on additions to a single pass over files the gates already read — proven by a read-counting test (files: engine/src/threepowers/conformance.py, engine/tests/test_conformance_binding.py)
- [x] T014 [HARDN-NFR-003] Preserve backward compatibility: rotation-free ledgers verify unchanged, software-key custody unchanged, pattern-less adapters degrade to visible quarantine (files: engine/src/threepowers/verify.py, engine/src/threepowers/keys.py, engine/src/threepowers/conformance.py, engine/tests/test_rotation.py, engine/tests/test_external_signer.py, engine/tests/test_conformance_binding.py)
- [x] T015 [HARDN-NFR-004] Hold the extended trust-spine modules to the High-risk bar: `anchor.py` joins the mutation scope; the engine passes the hardened gate suite at its declared tiers (files: engine/pyproject.toml, engine/tests/e2e/test_hardening_e2e.py)
- [x] T016 [HARDN-FR-001] Document the new commands and knobs across the user-facing docs + plan/tasks artifacts (files: docs/cli-reference.md, docs/STATUS.md, docs/threat-model.md, AGENTS.md, CLAUDE.md, CHANGELOG.md, plan/017-trust-hardening.md, specs/005-trust-hardening/tasks.md)
