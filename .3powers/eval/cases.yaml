# 3Powers' constitution, commands, and role config are versioned software with an eval
# set (3PWR-FR-050). `3pwr eval` runs this and BLOCKS on any regression — a change that
# drops a non-negotiable property fails here instead of shipping silently.
cases:
  - name: constitution-non-negotiables
    file: .specify/memory/constitution.md
    must_contain:
      - "Separation of Powers"
      - "Oracle Independence"
      - "different model family"
      - "hash-chained"
      - "never satisfied by weakening"

  - name: oracle-command-independence
    file: .github/agents/3pwr.oracle.agent.md
    must_contain:
      - "Read ONLY"
      - "different model family"
      - "property-based"

  - name: review-command-different-family
    file: .github/agents/3pwr.review.agent.md
    must_contain:
      - "different model family"
      - "intent fit"

  - name: roles-bind-model-families
    file: .3powers/config/roles.yaml
    must_contain:
      - "model_family"
      - "oracle"
      - "coder"

  # Portability (3PWR-A1/A3): 3Powers must run under any Spec Kit-dispatchable integration, not
  # Copilot-only. These block a regression to a provider-specific assumption (3PWR-FR-044/046).
  - name: roles-provider-agnostic
    file: .3powers/config/roles.yaml
    must_contain:
      - "any agent Spec Kit can dispatch"

  - name: extension-provider-agnostic
    file: .specify/extensions/3powers/extension.yml
    must_contain:
      - "3pwr.oracle"
      - "3pwr.verify"
      - "provider-agnostic"
      - "speckit_version"
    must_not_contain:
      - "integration: copilot"
