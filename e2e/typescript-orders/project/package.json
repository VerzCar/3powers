{
  "name": "@e2e/typescript-orders",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Order-pricing service — the TypeScript sample for the 3pwr e2e kit.",
  "main": "src/index.ts",
  "scripts": {
    "check": "biome format . && biome lint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --coverage",
    "mutation": "stryker run"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.3",
    "@stryker-mutator/core": "^9.2.0",
    "@stryker-mutator/vitest-runner": "^9.2.0",
    "@vitest/coverage-v8": "^3.2.6",
    "fast-check": "^3.23.1",
    "typescript": "^5.6.3",
    "vitest": "^3.2.6"
  },
  "overrides": {
    "qs": "^6.15.3"
  }
}
